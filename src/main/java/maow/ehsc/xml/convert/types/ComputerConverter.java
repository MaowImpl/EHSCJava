package maow.ehsc.xml.convert.types;

import maow.ehsc.xml.convert.AbstractDocumentConverter;
import org.dom4j.Document;
import org.dom4j.Element;

public class ComputerConverter extends AbstractDocumentConverter {
    public ComputerConverter(Document document) {
        super(document);
    }

    @Override
    protected Element doConversion() {
        return null;
    }
}
